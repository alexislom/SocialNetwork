<nav class="navbar navbar-default navbar-fixed-top header">
    <div class="container-fluid">
        <div class="navbar-header">
            <a class="navbar-brand" href="~/Home/">Home</a>
        </div>
        <ul class="nav navbar-nav navbar-right">

            @if (User.Identity.IsAuthenticated)
            {
                <li>
                    @Ajax.ActionLink("Search","Search","Profile",null,new AjaxOptions()
                    {
                        UpdateTargetId = "main_container",
                        LoadingElementId = "loading",
                        OnBegin = "hideContent",
                        OnComplete = "unhideContent"
                    })
                </li>
                <li class="dropdown">
                    <a class="dropdown-toggle" data-toggle="dropdown" role="button">@User.Identity.Name <span class="caret"></span></a>
                    <ul class="dropdown-menu" role="menu">
                        <li>@Ajax.ActionLink("Edit Profile","Edit","Profile",null,new AjaxOptions()
                            {
                                UpdateTargetId = "main_container",
                                LoadingElementId = "loading",
                                OnBegin = "hideContent",
                                OnComplete = "unhideContent"
                            })
                        </li>
                        <li class="divider"></li>
                        <li><a href="@Url.Action("LogOff", "Account")"><i class="glyphicon glyphicon-log-out"></i>Exit</a></li>
                    </ul>
                </li>
            }
            else
            {
                <li>@Html.ActionLink("Login", "Login", "Account")</li>
                <li>@Html.ActionLink("Register", "Register", "Account")</li>
            }
        </ul>
        </div>
</nav>




